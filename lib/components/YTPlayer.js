export default (client, id) => {  return client.hoc({    id,    mounted(props) {      const { videoId, width, height, Player, onPlaying, onMounted } = props;      const player = new Player(`#${videoId}`, {        height: String(height),        width: String(width),      });      player.load(videoId);      player.on('playing', () => onPlaying(player));      onMounted(player);    },    render({ props }) {      return (        <div id={props.videoId}/>      );    }  });};