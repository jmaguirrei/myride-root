export default (client, id) => {  return client.hoc({    id,    classes: {      logo: `        max-width: 60%;        margin: 12%;      `,      link: `        font-size: 20px;        padding: 16px;        cursor: pointer;        color: white;      `,      button: `        margin-top: 60px;        padding: 10px;        cursor: pointer;        width: 60%;        text-align: center;        color: white;        font-size: 20px;        text-shadow: 0px 1px 2px hsla(0, 0%, 0%, 0.8);        background: ${client.lib.Colors.BLUE_SIGNIN};        border-radius: 12px;        box-shadow: 0px 1px 1px -1px black;      `,    },    styles: {      menu: isMenuOpen => `        position: absolute;        display: flex;        flex-flow: column;        align-items: center;        width: 100%;        height: 100vh;        z-index: 10;        background: ${client.lib.Colors.GREY_DARK};        opacity: ${isMenuOpen ? 0.99 : 0};        pointer-events: ${isMenuOpen ? 'auto' : 'none'};        transition: opacity .4s ease;      `,    },    render({ props, styles, classes }) {      return (        <div id='menu' style={styles.menu(props.isMenuOpen)}>          <img src={props.logoSrc} class={classes.logo}/>          {            props.options.map(item => {              const { className, name, onclick } = item;              return (                <div class={classes[className]} onclick={onclick}>                  { name }                </div>              );            })          }        </div>      );    }  });};