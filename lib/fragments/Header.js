export default (client, id) => {  return client.hoc({    id,    classes: {      header: `        display: flex;        align-items: center;        width: 100%;        justify-content: center;        height: ${client.lib.Sizes.HEADER_HEIGHT};        background: ${client.lib.Colors.GREY_DARK};      `,    },    styles: {      logo: isMenuOpen => `        opacity: ${isMenuOpen ? 0 : 1};        pointer-events: ${isMenuOpen ? 'auto' : 'none'};        max-height: 50%;      `,    },    render({ props, classes, styles }) {      return (        <div id='header' class={classes.header}>          <img src={props.logoSrc} style={styles.logo(props.isMenuOpen)}/>        </div>      );    }  });};